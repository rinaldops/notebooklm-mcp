# Fora do .vsix: fontes e configs de build. O `vendor/` (servidor + deps de
# produção) É INCLUÍDO — é o que torna o pacote autossuficiente.
src/**
scripts/**
tsconfig.json
tsup.config.ts
.vscodeignore
**/*.map
**/.cache/**
